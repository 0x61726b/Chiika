os:
  - linux


branches:
  only:
    - master

env:
  - NODE_VERSION="4.2.1"

before_install:
 - chmod +x ./travis-build.sh

script:
  - gulp compile:scripts
  - ./travis-build.sh

cache:
  directories:
    - node_modules
